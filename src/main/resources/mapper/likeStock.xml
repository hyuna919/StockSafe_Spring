<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.LJ.StockSafe.model.mapper.LikeStockMapper">

	<select id="readLikeStock" parameterType="string" resultType="UserStockDto" >
		select *
		from likeStock
		where member_id = #{memberId}
	</select>
	
	<insert id="createLikeStock" parameterType="UserStockDto" useGeneratedKeys="true"
    keyProperty="id">
		insert into likeStock(member_id, stock_id)
		select #{memberId}, #{stockId} from dual
		where not exists(
			select member_id, stock_id from likeStock
			where member_id=#{memberId} and stock_id = #{stockId})
		
	</insert>
	
	<delete id="deleteLikeStock" parameterType="String">
		delete
		from likeStock
		where member_id=#{memberId} and stock_id=#{stockId};
	</delete>

</mapper>