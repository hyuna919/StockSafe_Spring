<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--  int createUser(UserinfoDto userinfodto);
	List<UserinfoDto> readUserAll(String key, String word); 
	UserinfoDto readUser(String id); 
	int loginUser(String id, String userinfo_pw);
	int updateUser(String id);
	int deleteUser(String id);
	
	
		<if test="key == 'id'">
				where id like concat('%', #{word}, '%')
		</if>
 -->

<mapper namespace="com.LJ.StockSafe.model.mapper.MemberMapper">
	
	<select id="readUserAll" parameterType="map" resultType="MemberDto">
		select *
		from member
		where id=#{key}
	</select>
	
</mapper>
	