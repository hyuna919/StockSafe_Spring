<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--  int createUser(UserinfoDto userinfodto);
	List<UserinfoDto> readUserAll(String key, String word); 
	UserinfoDto readUser(String id); 
	int loginUser(String id, String userinfo_pw);
	int updateUser(String id);
	int deleteUser(String id);
 -->

<mapper namespace="com.LJ.StockSafe.model.mapper.UserinfoMapper">
	
	<insert id="createUser" parameterType="UserinfoDto">
		insert into userinfo(id, userinfo_pw, userinfo_name, userinfo_email_id, userinfo_email_domain, userinfo_regdate)
		VALUES( #{id}, #{userinfo_pw}, #{userinfo_name}, #{userinfo_email_id}, #{userinfo_email_domain}, now()));
	</insert>
	
	<select id="readUserAll" parameterType="String" resultType="UserinfoDto">
		select *
		if
		from userinfo
		<if test="#{key} == 'id'">
				where id like concat('%', #{word}, '%')
		</if>
		<if test="#{key} == 'userinfo_name'">
				where userinfo_name like concat('%', #{word}, '%')
		</if>
		<if test="#{key} == 'userinfo_email_domain'">
				where userinfo_email_domain like concat('%', #{word}, '%')
		</if>
	</select>
	
	<select id="readUser" parameterType="String" resultType="UserinfoDto">
		select *
		from userinfo
		where id = #{id}
	</select>
	
	<select id="loginUser" parameterType="String" resultType="String">
		select userinfo_name
		from userinfo
		where id = #{id} and userinfo_pw = #{userinfo_pw}
	</select>
	
	<update id="updateUser" parameterType="UserStockDto">
		update userinfo 
		set userinfo_name = #{userinfo_name},
		userinfo_email_id = #{userinfo_email_id},
		userinfo_email_domain = #{userinfo_email_domain}
		where id = #{id}	
	</update>
	
	<delete id="deleteUser" parameterType="UserStockDto">
		delete from userinfo
		where id = #{id}
	</delete>
	
</mapper>
	